package net.shuyanmc.mpem.optimization;
/*
import net.minecraftforge.eventbus.api.Event;
import net.minecraftforge.eventbus.api.IEventListener;
*/

/**
 * 用于替代ASM生成类的占位处理器
 */
public final class EventPlaceholder/* implements IEventListener*/ {
    /*
    public static final EventPlaceholder INSTANCE = new EventPlaceholder();
    public static final Constructor<?> CONSTRUCTOR_DUMMY;

    static {
        try {
            CONSTRUCTOR_DUMMY = EventPlaceholder.class.getConstructor(Object.class);
        } catch (NoSuchMethodException e) {
            throw new RuntimeException(e);
        }
    }

    // 提供两种构造器以匹配Forge的不同使用场景
    public EventPlaceholder() {}
    public EventPlaceholder(Object ignored) {}

    @Override
    public void invoke(Event event) {
        // 空实现，实际处理逻辑会被替换
    }*/
}